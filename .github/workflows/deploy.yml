name: CI/CD (Vite + Go)

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Versi pnpm diambil dari package.json#packageManager (jangan set "version" di sini)
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: apps/api-go/go.sum

      - run: pnpm install

      - run: pnpm db:generate
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/test

      - name: Build shared
        run: pnpm --filter @dakwah/shared build

      - name: Build Vite SPA
        run: pnpm --filter @dakwah/web-vite build
        env:
          # CI compile-only; nilai produksi di-set di apps/web-vite/.env di VPS
          VITE_API_URL: https://api.binaisi.xyz/api/v1

      - name: Build Go API
        working-directory: apps/api-go
        run: |
          mkdir -p bin
          go build -o bin/aisi-api ./cmd/server

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 30m
          script: |
            set -euo pipefail
            cd /opt/aisi
            # Pastikan /opt/aisi milik user deploy (hindari clone sebagai root)
            if [[ ! -w .git/objects ]]; then
              echo "ERROR: .git tidak writable oleh $(whoami). Jalankan di VPS:"
              echo "  sudo chown -R deploy:deploy /opt/aisi"
              exit 1
            fi
            git fetch origin main
            git reset --hard origin/main
            set -a
            # shellcheck disable=SC1091
            source apps/api-go/.env
            set +a
            bash deploy/deploy.sh
            curl -sf http://127.0.0.1:4000/health
