# Salin ke apps/web-vite/.env (atau set di CI build-time)
# Build-time: nilai ini di-bake ke bundle — rebuild setelah mengubah.

# API di subdomain (Opsi B)
VITE_API_URL=https://api.binaisi.xyz/api/v1
